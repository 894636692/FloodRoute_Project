# 动态数据字段定义核实（2026-09-18）

本步只核实官方说明，不生成最终 CSV。结论：已有真实下载样本，但当前可读取的证据不足以确认时间时区、采集时间含义和水位测量基准。`dynamic_data_inventory.csv` 中的 `blocked` 仍准确，未重复修改清单。

## 本次访问记录

| 公开来源 | 本次结果 | 证据边界 |
|---|---|---|
| [积涝点水位 API 详情页](https://opendata.sz.gov.cn/data/api/toApiDetails/29200_01403147) | 网页读取工具返回页面框架、登录区域及页脚，未取得接口字段说明；本机 Python 直接请求返回 HTTP 403 | 不能认定接口不存在，也不能断言登录后没有文档；未调用受保护的数据 API |
| [常见问题](https://opendata.sz.gov.cn/interaction/question/toQAA) | 返回页面框架，未取得问题正文 | 不能据此认定官方没有解释 |
| [操作指南 PDF](https://opendata.sz.gov.cn/static/opendata1.0/doc/userbook.pdf) | 网页读取因文件超过工具大小限制失败（12,798,718 字节）；本机直接请求返回 HTTP 403 | 未读到 PDF 内容，不对其中有无相关说明作结论 |

未使用浏览器凭据，未绕过登录，未向平台提交任何消息。本记录只描述本次能取得的证据，不把读取失败解释为数据不可用。

## 用户提供的官方页面截图补证

用户随后提供两张该 API 详情页截图，已能读取接口服务文档。以下为截图证据，不代表已经成功调用 API。第一张截图的请求示例含 appKey，本记录不抄录该值，也不将截图复制进仓库。

| 项目 | 截图确认内容 | 使用边界 |
|---|---|---|
| 页面数据量 | 36,992,162 条；页面更新日期 2026-09-18 | 是页面所示统计，不等于已下载数量，不证明历史连续或最早观测日期 |
| 接口地址 | `https://opendata.sz.gov.cn/api/2920001403147/1/service.xhtml` | 已确认文档地址；实际调用尚未验收 |
| 格式 | JSON | 不保证 API 响应与下载包中的 JSON 文件结构相同 |
| 调用流程 | 提交应用 → 订阅接口 → 测试接口 | appKey 必填，文档称申请通过后可在个人中心查看；尚未确认用户当前订阅/审批状态 |
| 分页 | page、rows 均为必填整数；页码说明称通常从 1 开始；每页最多 10,000 条 | API 单页上限不等于网页文件下载上限；不能据此认定文件最多导出 100,000 条 |
| 日期筛选 | startDate、endDate 为可选字符串，格式 `yyyymmdd`；说明明确为对应时间范围内的“入库数据” | 不能当作 SJ 观测时间过滤；时区、端点是否包含、默认排序与分页稳定性未说明 |
| 返回字段 | ID：水位 id；CZBM：测站编码；SJ：时间；SW：水位（m） | SJ 时区/时间语义、水位零点仍未确认 |

日期筛选和分页机制已有文档证据，但历史可获取最早日期、是否所有页面都可取到，仍需授权后的少量 API 实测或官方说明。上架日期 2019-05-06 不能作为最早观测日期。

## 已确认与待确认

下载包的详细核验见 [水位验收记录](water_level_download_check_2026-09-18.md) 和 [站点验收记录](stations_download_check_2026-09-18.md)。

| 项目 | 已确认 | 仍需官方说明 |
|---|---|---|
| 关联键 | 水位 CZBM 对应站点 STCD；样本 148 个站全部匹配内涝水情站 | 无 |
| 单位 | 原字段标注 m | 水位相对于哪个零点；是否确实表示地面以上积水深度 |
| 时间 | 字段描述称“采集时间”，原始值不含时区 | SJ 是否为传感器观测时刻；采用 UTC 还是北京时间等明确时区 |
| 历史 | 快照有 100,000 行，源时间覆盖 2026-09-15 21:30:32 至 2026-09-18 00:57:23；截图确认 API 支持按入库日期筛选及分页 | 文件导出是否截断、最早可获取日期、实际历史分页可用性；本次文件下载筛选条件也尚未确认 |
| 更新 | 目录写“不定期”或“有数据更新时” | 测站采样周期，与平台发布延迟需分别说明 |
| 重复 | 8,512 个同站同刻组合涉及 18,517 行，值一致但水位 id 不同 | 是重复发布、补录、修订还是其他含义；先全部保留 |
| 空间 | 下载名录没有经纬度；官方资源描述称坐标不宜公开 | 是否有可申请用于研究的坐标或可合法使用的替代空间信息；不自行猜测位置 |

米转换厘米的数学关系是乘以 100，但单位换算不会消除测量基准的未知，也不会把一般水位变成道路积水深度。

## 可复制的咨询草稿（尚未发送）

主题：市水务局积涝点水位数据字段定义与历史范围咨询（29200/01403147）

您好，我们正在开展城市内涝数据研究，已从深圳市政府数据开放平台下载“市水务局积涝点水位数据”（29200/01403147）及“市水务局测站基本信息表”（29200/01400987）。为准确使用数据，烦请提供字段字典或确认以下事项：

1. SJ（时间）是否代表传感器实际观测时刻，还是平台采集/入库时刻？其时区是否为北京时间 UTC+08:00？
2. “水位（m）”的测量零点是什么？是相对道路地面的积水深度，还是相对传感器零点或其他高程基准的水位？不同测站是否采用相同定义？
3. 此次文件导出恰好有 100,000 条。单次文件下载是否有条数上限？API 文档说明 startDate/endDate 筛选入库日期，其时区和日期端点包含规则是什么，与 SJ 有何关系？最早可以获取到哪一天，分页是否有稳定排序？
4. 测站通常多久采集一次，开放平台通常多久发布一次？是否有时间戳或状态字段用于区分延迟、补报、缺测？
5. 下载中存在测站编码和时间相同、水位相同但水位 id 不同的记录。这些记录表示重复发布、版本修订还是独立记录？推荐如何保留或使用？
6. 站点资源说明经纬度不宜公开。是否有合规申请研究用途坐标的途径，或可公开用于空间关联的其他站点信息？

烦请尽可能提供官方说明链接或书面字段定义，谢谢。

平台页脚公开联系邮箱为 `opendata@sz.gov.cn`（见上方 API 详情页）；这只是咨询渠道，不代表已联系或获得答复。

## 元数据标签截图补证

用户随后提供了元数据标签截图，资源编号仍为 `29200/01403147`，名称为“市水务局积涝点水位数据”。截图确认：

- 数据提供方、维护方均为深圳市水务局；资源状态为“激活”。
- 开放方式为“无条件开放”，更新频率为“有数据更新时”。开放分类不能代替 API 文档明确要求的 appKey 和订阅流程。
- 上架日期为 2019-05-06，更新日期为 2026-09-18。这是目录日期，不能当作历史观测起止时间。
- 时间范围为“暂无统计”，空间（地域）范围为“暂无空间信息”。前者不代表没有历史记录，后者不代表这些站点没有实际位置。
- 摘要仍只称“采集时间、水位”，没有补充时区、实际观测/入库时间区别、水位测量零点或高程基准。
- 页面资源格式列为 CSV、XLS、JSON、XML；实际下载包格式仍以已验收文件为准。

至此，用户提供的接口服务文档及元数据截图已核验。时间语义、时区和水位基准仍是标准化阻塞项，不从站点所属城市推断时区，也不从“积涝点”名称推断测量零点。清单中的 blocked 状态继续有效。

## 当前教学检查点

页面说明核验已完成。下一小步：检查此接口的订阅/审批状态。用户可从文档“订阅接口”入口查看提示，只需反馈已订阅、待审核、需先提交应用等状态，不需提供 appKey。若入口会立即提交申请，先记录当前页面状态，不由助手代为提交。

取得合法调用权限后，可先进行单页少量响应测试并原样保存，以确认响应结构和实际访问可用性；这不要求预先猜测时区，也不意味着标准接口已通过验收。当前没有发起 API 测试或批量历史下载。

若页面仍没有定义，可由用户自行提交上述草稿或明确授权代为发送。等待证据期间保持真实标准接口为 blocked；现有 B 联调 fixture、原始数据、GIS 与算法代码均保持原状。
