# 深圳实况格点两表下载验收

最新补充：用户已确认北京时间、截至 FORECASTTIME 向前对应小时累计，已据此生成[真实降雨候选接口](shenzhen_rainfall_candidate.md)。该确认来源为用户，不冒称官方独立核验。下文保留原始检查及逐步补证历史；当前状态以候选接口说明及动态数据清单为准。

最新结论：两份真实 CSV 已原样归档，字段及格网关联检查通过；后续官方页面截图已确认格网坐标是 WGS84（EPSG:4326），无需坐标转换。最终降雨接口仍因时区、观测时间字段、累计窗口和精确获取时间缺失而 blocked。本次是文件可用性验收，不是认定所有数值已物理有效。

## 后续截图补证（2026-09-18 14:38 +08:00）

用户提供的格点信息表页面“数据简介”明确写明 WGS84；数据项说明 X1/Y1 为左下角经度/纬度，X2/Y2 为右上角经度/纬度。两表关联为 **实况数据表 GRIDID → 格点信息表 RECID**；数据表自身 RECID 表示记录编号，不能拿两表 RECID 直接相连。

第二张截图确认 DDATETIME=发布时间、FORECASTTIME=预报时间、YBSX=预报时效（小时）、PLEVEL=预报级别。截图未说明时区，也未说明零时效实况时应选哪个字段作为观测时间；不能因此补填 +08:00。降雨字段所在的数据项下半部分及元数据尚未提供。

两张截图按原字节保存在 `data/raw/dynamic/rainfall/metadata_20260918T143826500292+0800/`，哈希及字段映射见同目录 evidence.json。最新复核报告：`outputs/dynamic/shenzhen_grid_metadata_20260918T143826500292+0800/quality_report.json`，旧报告保留作为获取证据前的快照。下面检查表中“CRS 未确认”属于初次验收记录，现已由本段证据更新。此 WGS84 结论仅适用于深圳气象格网，不适用于水务测站或 NOAA。

## 输入与来源

用户提供的下载文件：

- `C:/Users/Gao/Downloads/深圳范围自动站实况格点数据表20260918022309147543/深圳范围自动站实况格点数据表_2920000903509.csv`
- `C:/Users/Gao/Downloads/深圳范围自动站实况格点信息表20260918022338444244/深圳范围自动站实况格点信息表_2920000903510.csv`

官方资源：[数据表](https://opendata.sz.gov.cn/data/dataSet/toDataDetails/29200_00903509)、[格点信息表](https://opendata.sz.gov.cn/data/dataSet/toDataDetails/29200_00903510)。用户提供页面截图及对应 CSV 文件名确认资源名称和编号。文件不包含 CRS、时区及字段完整释义；未获得本批元数据文件。

气象局[格点关联说明](https://weather.sz.gov.cn/mobile/hudongjiaoliu/yewuzhishiku/qixiangfuwulei/content/post_12737917.html)支持用格网编号连接两张表。格点不是实体站点，不能据此声称有 4,232 个雨量站。插值性质来源见[前次来源核查](rainfall_source_check_2026-09-18.md)。本次公开检索未找到能明确本资源时区/坐标基准的新增证据。

## 原件保护

归档目录：`data/raw/dynamic/rainfall/shenzhen_grid_20260918T142832315164+0800/`，保存上述两个同名 CSV 和 archive_receipt.json。源文件与副本逐字节校验一致。

| 文件 | 字节数 | SHA256 |
|---|---:|---|
| 数据表 | 31,217,386 | 21f6ae170a34b8c4c2e5e8ac018209fbd988969dd913186f5fdbf104bd4c6d71 |
| 信息表 | 202,868 | 6d53455a9c65757e553e34a3937a02a1e680d0a642405c41b697f0d6f79eae16 |

archived_at=2026-09-18T14:28:32.315164+08:00 仅表示本地归档时刻。retrieved_at 留空；不把文件名或磁盘修改时间当成精确网站获取时间。

## 检查结果

| 检查 | 结果与含义 |
|---|---|
| 规模 | 数据表 100,000 行、23 列；格点信息表 4,232 行、8 列 |
| 格点主键 | 4,232 个唯一 ID，无空值、无重复 |
| 外键关联 | 数据表 100,000 行全部匹配，未丢行，也不产生多对多扩张 |
| 候选记录键 | 格网 ID + 源预报时间无重复；记录编号无重复 |
| 文本空值 | 两表所有列空字符串数量均为 0；不等于不存在特殊缺失码 |
| 角点字段 | 左下、右上经纬度均可解析，范围与角点顺序检查通过；CRS 未确认 |
| 原角点包络 | 经度 113.7333–114.6533，纬度 22.4–22.86；不声明为 WGS84，不进行道路匹配 |
| 源预报时间/发布时间 | 2026-09-17 20:50:00 至 2026-09-18 00:40:00，二者逐行相同，均无时区 |
| 源入库时间 | 2026-09-17 20:53:03 至 2026-09-18 00:48:44，无时区 |
| 时次覆盖 | 24 个时次，相邻间隔 10 分钟；这是样本间隔，不是已证实的长期发布频率 |
| 最早时次 | 2,664 个格点，比本名录少 1,568 个；其余 23 时次各 4,232 个 |
| 预报时效/级别 | 全部为 0；其业务含义未获得官方字段定义 |

样本仅有 3 小时 50 分钟的源时次跨度，不是所选 9 月 15–18 日完整历史。页面提示“前 1 万条”，实际文件却为 10 万行；记录这一差异，不能据此声称已查明下载上限。最早时次不全与截断相容，但目前不能排除其他原因，也不能说平台本身缺测。

## 降雨字段

| 字段 | 实际值 | 可确认的单位/时段 |
|---|---|---|
| 1 小时累计 | 全部 0 | mm，60 分钟（来自原列名） |
| 2 小时累计 | 全部 0 | mm，120 分钟 |
| 3 小时累计 | 全部 0 | mm，180 分钟 |
| 6 小时累计 | 全部 0 | mm，360 分钟 |
| 24 小时累计 | 0–0.3，3,311 行大于 0 | mm，1,440 分钟 |
| Tracer1小时累计降雨量预报 | 全部 -1 | 预报字段；缺失码含义未确认，不能解释为实况负降雨或改为 0 |

以上五个累计量字段均可解析，无负值。10 分钟时次间隔不意味着降雨是“10 分钟累计”；也不能把相邻 1 小时累计值直接相加为总雨量。累计窗口以哪个时间字段为锚点、向前还是向后、是否滚动，仍需官方说明。当前小时雨量全零的样本能测试文件读取和关联，不能验证强降雨过程下的算法表现。

## 代码、复现与测试

脚本：`scripts/inspect_shenzhen_rainfall_download.py`。示例在项目根目录执行：

```powershell
.\.venv\Scripts\python.exe scripts\inspect_shenzhen_rainfall_download.py --data "原始数据表的绝对路径" --grids "原始格点信息表的绝对路径"
.\.venv\Scripts\python.exe -m unittest discover -s tests\dynamic -v
```

脚本只做归档和检查，每次新建时间戳目录，不写 derived、不进行坐标转换或补时区。实际报告：`outputs/dynamic/shenzhen_grid_20260918T142832315164+0800/quality_report.json`。新增 4 项测试覆盖主键重复/外键缺失、坏时间/坐标/非有限数值、重复候选键与输入保护；20 项动态测试全部通过。程序未触碰原有 GIS、路线代码和 fixture。

## 下一步需要的官方说明

请从两张表页面的“数据项”“元数据”或“调用数据接口”文档提供：

1. 已解决：角点经纬度为 WGS84，采用 EPSG:4326，无转换。
2. 预报时间/发布时间/入库时间的时区；哪个字段代表实况有效/观测时间。
3. 预报时效=0、预报级别=0 的含义；1 小时累计雨量相对于哪个时间、窗口如何定义。
4. Tracer 字段 -1 的含义及导出时间过滤/条数限制（可后续补充）。

若页面未说明，保留截图并标记“未说明”，不要推断。已有格网角点不能直接生成真实 stations.csv；格网坐标契约需明确采用角点或代表点并与 A/B 协调。预报字段也不提供 member_id，不能当作集合预报。当前两份样本足够继续核对定义，无需立即重复下载或申请 API。
