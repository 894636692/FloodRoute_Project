# FloodRoute 最终研究状态

日期：2026-09-21  
工程稳定版：`main` / `v1.2.0`  
研究验证版：`research-validation/v1.3` / `v1.3.0-research`

1. **Release tag**：工程发布标签 `v1.2.0`；研究冻结标签 `v1.3.0-research`。
2. **Release commit**：v1.2.0 对应 `0f4f9a2b5e83a9ec0f051a6826731e749d356434`。研究标签固定 Phase B/C 与综合文档，未覆盖 v1.2.0。
3. **CI 状态**：GitHub Actions Windows / Python 3.13 / Git LFS / lock 依赖通过，运行 `35588251822` 成功。
4. **单元测试数量**：最终 `python -m pip check` 通过；`python -m unittest discover -s tests -q` 共 110 项通过。没有删除原 105 项测试。
5. **Chrome 人工验收状态**：`manual_visual_review = passed`。真实用户 Google Chrome 核心操作通过，控制台错误 0；未单独测试的显示器/硬件组合仍为 pending。
6. **历史事件**：2023-09-07 傍晚至 09-08 深圳极端降雨与城市内涝事件。
7. **历史数据来源**：事件事实和灾情来自深圳市水务、应急、交通、区政府等官方公开资料；降雨强迫使用 NASA POWER MERRA-2 `PRECTOTCORR` 小时数据，标记为 `REAL_HISTORICAL_COARSE_FORCING`，不是道路实测。
8. **可定位灾情样本数量**：审计 10 条弱标签；1 条道路段同时满足可审计、在正式路网内和主阈值匹配条件。其余 9 条保留但不强行量化。
9. **历史验证主要结果**：26 时次 × 112,218 道路，共 2,917,668 条风险记录。唯一主样本中位风险百分位 38.86%，coverage@top-5/10/20 均为 0；报告道路平均风险 0.3270，5 条匹配背景道路为 0.3588。结果没有支持该样本的高风险排序能力。
10. **扩展验证场景数**：3 个新家族，共 36 个 `family × seed` 场景实例。
11. **扩展验证 seed 数**：12 个新种子，固定为 `7101–7112`。
12. **OD 数**：8 组，以固定 seed `7201` 按方向和 3–15 km 有向最短距离自动生成，不读取风险结果。
13. **路线决策总数**：27,648 条；6,912 个配对观测组，观测配对违规 0。它们是重复测量，不是 27,648 个独立样本。
14. **方法比较结果**：risk − shortest 平均差 −0.023015，seed-level bootstrap 95% CI `[−0.023348, −0.022710]`；risk_uncertainty − risk 为 +0.000822；trusted − risk 为 +0.000563；trusted − risk_uncertainty 的 CI 跨 0。风险优先在本受控测试总体优于最短路，但可信优先不是稳定赢家。
15. **Trigger 结果**：36 个事件中，never/always/triggered 平均暴露为 0.462578/0.452697/0.458775；平均 planner calls 为 1/12/2.056。触发式减少计算，但 7 个事件错过有用重规划；无预注册的频繁搜索或振荡案例。
16. **得到支持的创新点**：真实 GIS 与动态数据的可重复风险/路由链路；持久 Leaflet 增量交互；Ground Truth/灾情弱标签隔离；受控条件下风险规划相对最短路降低暴露；Trigger 的计算与暴露折中；输入、协议、结果和复现哈希审计。
17. **未得到充分支持的创新点**：历史事件道路排序能力；可信优先总体最优；Freshness 的独立现实安全收益；Uncertainty 必然降低暴露；Trigger 在每个事件都及时换路；现实道路安全保证。
18. **所有剩余限制**：道路风险是未校准指数；真实水位因坐标、时间语义和测量基准不足保持关闭；历史强迫粗且弱标签仅 1 个主样本；未报道道路不是安全负样本；缺少排水管网、道路结构、临时管制、转向限制和实时交通；受控模拟不能代表所有暴雨；在线底图依赖网络；系统不能直接作为真实应急导航。

## 冻结与可复现性

- 冻结配置：`config/selected_v1_1.json`
- 原文件 SHA256：`3f8ea08239d12d19b801afec28b5156625dd08daef4f54ff72573756bd7e6dd5`
- `water.active=false`
- 历史验证完整重复：通过，非耗时结果逐字节一致。
- 扩展验证完整重复：通过；排除环境计时列后，routes、aggregates、bootstrap 输入/输出和 Trigger 非计时指标完全一致。
- `main` 保持 v1.2.0；研究验证不修改或移动 `v1.2.0` 标签。

本状态用于等待人工复核，不再增加 UI 或重新调整 Risk、Routing、Freshness、Uncertainty、Trigger 参数。
